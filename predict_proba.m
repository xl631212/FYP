function result = predict_proba(object, test_data, train_data)
    result = [];
    for i = 1:size(test_data)
        wtx = 0;
        w = {};
        % featrue vector
        for index = 1: size(test_data,2)
            if z > 1 
                sign = 1;
            end
        end
    end

end